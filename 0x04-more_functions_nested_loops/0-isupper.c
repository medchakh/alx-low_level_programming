#include "main.h"
#include <stdio.h>

/**
 * print_numbers -  a function that prints the numbers, from 0 to 9.
 * no variable input
 *
 * Ruturn: void
 */
 
 int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
		return (1);
	else
		return (0);
}
