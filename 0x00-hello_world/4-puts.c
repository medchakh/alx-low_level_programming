#include <stdio.h>

/**
 * main - is Entry point
 *
 * The function will return  a string
 * Return: Always 0 
 */
int main(void)
{
char *a = "\"Programming is like building a multilingual puzzle";

puts(a);

return (0);

}